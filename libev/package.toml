[package]
name = "libev"
version = "4.33"
description = "High performance event loop library in C"

[source]
url = "https://dist.schmorp.de/libev/libev-4.33.tar.gz"
sha256 = ""

[dependencies]
deps = [""]

[build]
system = "automake"
compile_flags = ["--disable-static", "--prefix=/usr"]
